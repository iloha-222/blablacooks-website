---
import Layout from '../layouts/Layout.astro';
import '../styles/global.css';
---

<Layout
  title="Blablacooks - Les ateliers qui font parler"
  description="Blablacooks propose des ateliers de cuisine participative, couture et français interculturel à Montpellier. Rejoignez-nous pour partager, apprendre et créer des liens."
>

  <!-- ═══════════════════════════════════════════════════════════════════
       HERO SECTION - Asymmetric composition with floating characters
  ═══════════════════════════════════════════════════════════════════ -->
  <section class="hero">
    <!-- Background elements -->
    <div class="hero-bg">
      <div class="blob blob-1"></div>
      <div class="blob blob-2"></div>
      <div class="hero-pattern"></div>
    </div>

    <div class="hero-container">
      <!-- Left content -->
      <div class="hero-content">
        <span class="hero-label animate-fade-up">Association interculturelle • Montpellier</span>
        <h1 class="hero-title animate-fade-up delay-1">
          Les ateliers<br/>
          <span class="hero-title-accent">qui font parler</span>
        </h1>
        <p class="hero-desc animate-fade-up delay-2">
          Découvrez la cuisine, la couture à travers le prisme de l'échange interculturel en français. Partagez, apprenez, rencontrez.
        </p>
        <div class="hero-actions animate-fade-up delay-3">
          <a href="#activites" class="btn btn-primary">
            Découvrir nos ateliers
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
          </a>
          <a href="/a-propos" class="btn btn-secondary">En savoir plus</a>
        </div>
      </div>

      <!-- Right: Character illustration composition -->
      <div class="hero-visual">
        <div class="hero-characters">
          <!-- Quinconce: 3 en haut, 3 en bas -->
          <div class="char-row char-row-top">
            <div class="char animate-float">
              <img src="/perso-nationalites/homme-blond-cuisine.webp" alt="" />
            </div>
            <div class="char animate-float-delayed">
              <img src="/perso-nationalites/Japonaise.webp" alt="" />
            </div>
            <div class="char animate-float">
              <img src="/perso-nationalites/femme-couture.webp" alt="" />
            </div>
          </div>
          <div class="char-row char-row-bottom">
            <div class="char animate-float-delayed">
              <img src="/perso-nationalites/Ivoirien.webp" alt="" />
            </div>
            <div class="char animate-float">
              <img src="/perso-nationalites/homme-couture.webp" alt="" />
            </div>
            <div class="char animate-float-delayed">
              <img src="/perso-nationalites/Bresilienne.webp" alt="" />
            </div>
          </div>
        </div>

        <!-- Decorative circle behind -->
        <div class="hero-decor">
          <div class="decor-circle-bg"></div>
        </div>
      </div>
    </div>

  </section>

  <!-- ═══════════════════════════════════════════════════════════════════
       MISSION SECTION - Full-width statement with photos
  ═══════════════════════════════════════════════════════════════════ -->
  <section class="mission">
    <div class="mission-bg grain"></div>

    <div class="container">
      <div class="mission-content" data-animate>
        <div class="mission-icon">
          <img src="/icons/noun-globe-6688733.svg" alt="" width="48" height="48" />
        </div>
        <h2 class="mission-title">
          Créer des ponts entre les cultures, un atelier à la fois
        </h2>
        <div class="divider divider-center"></div>
        <p class="mission-text">
          Blablacooks favorise la création de liens interculturels et participe à l'inclusion sociale par la mise en lumière et le partage des richesses culturelles et des savoir-faire des nouveaux arrivants dans la région de Montpellier.
        </p>
      </div>

      <!-- Photo mosaic -->
      <div class="mission-photos" data-animate>
        <div class="photo-card photo-1">
          <img src="/vrais-photos/a propos.jpg" alt="Atelier de cuisine" />
          <div class="photo-overlay"></div>
        </div>
        <div class="photo-card photo-2">
          <img src="/vrais-photos/a propos-.webp" alt="Participants" />
          <div class="photo-overlay"></div>
        </div>
        <div class="photo-card photo-3">
          <img src="/vrais-photos/Couture.webp" alt="Atelier de couture" />
          <div class="photo-overlay"></div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════════
       ACTIVITIES SECTION - 3 distinctive cards
  ═══════════════════════════════════════════════════════════════════ -->
  <section id="activites" class="activities section-lg">
    <div class="container">
      <div class="section-header">
        <span class="label label-terracotta">Nos activités</span>
        <h2>2 façons de se rencontrer et un cours de Français Langue Étrangère</h2>
        <p class="section-desc">Chaque atelier est une invitation au voyage et à l'échange</p>
      </div>

      <div class="activities-grid">
        <!-- Cuisine Card -->
        <a href="/ateliers-cuisine" class="activity-card activity-cuisine" data-animate>
          <div class="activity-visual">
            <div class="activity-img-wrapper">
              <img src="/vrais-photos/atelier de cuisine.webp" alt="Atelier cuisine" class="activity-img" loading="lazy" />
            </div>
            <div class="activity-badge">Cuisine</div>
          </div>
          <div class="activity-content">
            <h3>Ateliers Cuisine</h3>
            <p>Explorez les saveurs du monde avec des personnes venues des 4 coins du globe. Chaque atelier est un voyage culinaire.</p>
            <div class="activity-tags">
              <span>Brésil</span>
              <span>Cameroun</span>
              <span>Ukraine</span>
              <span>+15</span>
            </div>
            <span class="activity-link">
              Découvrir les ateliers
              <span class="arrow">→</span>
            </span>
          </div>
        </a>

        <!-- Couture Card -->
        <a href="/ateliers-couture" class="activity-card activity-couture" data-animate>
          <div class="activity-visual">
            <div class="activity-img-wrapper">
              <img src="/vrais-photos/atelier de couture.webp" alt="Atelier couture" class="activity-img" loading="lazy" />
            </div>
            <div class="activity-badge">Couture</div>
          </div>
          <div class="activity-content">
            <h3>Ateliers Couture</h3>
            <p>Initiez-vous à la couture dans un groupe interculturel. Créez, apprenez et partagez dans une ambiance conviviale.</p>
            <div class="activity-tags">
              <span>Initiation</span>
              <span>Réparation</span>
              <span>Création</span>
            </div>
            <span class="activity-link">
              Voir les ateliers
              <span class="arrow">→</span>
            </span>
          </div>
        </a>

        <!-- Français Card -->
        <a href="/francais-interculturel" class="activity-card activity-francais" data-animate>
          <div class="activity-visual">
            <div class="activity-img-wrapper">
              <img src="/vrais-photos/atelier linguistique other.webp" alt="Atelier français" class="activity-img" loading="lazy" />
            </div>
            <div class="activity-badge">Français</div>
          </div>
          <div class="activity-content">
            <h3>Français Interculturel</h3>
            <p>Pratiquez le français dans un cadre convivial et multiculturel. Conversation, échanges et bienveillance.</p>
            <div class="activity-tags">
              <span>Niveau A1</span>
              <span>Conversation</span>
              <span>Échange</span>
            </div>
            <span class="activity-link">
              En savoir plus
              <span class="arrow">→</span>
            </span>
          </div>
        </a>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════════
       UPCOMING EVENTS - Featured next workshops
  ═══════════════════════════════════════════════════════════════════ -->
  <section class="upcoming section">
    <div class="upcoming-bg">
      <div class="upcoming-pattern"></div>
    </div>

    <div class="container">
      <div class="section-header section-header-light" data-animate>
        <span class="label label-gold">Prochainement</span>
        <h2>Nos événements à venir</h2>
        <p class="section-desc">Réservez votre place pour nos prochains ateliers</p>
      </div>

      <div class="events-grid">
        <!-- Event 1: Cuisine Cambodge Occitanie -->
        <article class="event-card" data-animate>
          <div class="event-image">
            <img src="/vrais-photos/atelier de cuisine.webp" alt="Cueillette et Cuisine Cambodge Occitanie" loading="lazy" />
            <div class="event-status status-cuisine">Bientôt</div>
          </div>
          <div class="event-body">
            <div class="event-meta">
              <span class="event-category cat-cuisine">
                <img src="/icons/noun-cooking-pot-6972194.svg" alt="" class="event-icon" />
                Cuisine
              </span>
              <span class="event-date">23 Mai · 9h30-14h30</span>
            </div>
            <h3 class="event-title">Fusion Cambodge Occitanie</h3>
            <p class="event-desc">Cueillette et cuisine avec Malyne Neang. Samlor mchou kroeung aux herbes locales et gâteau au riz gluant.</p>
            <div class="event-details">
              <div class="detail">
                <img src="/icons/noun-location-8077585.svg" alt="" class="detail-icon" />
                Pignan
              </div>
            </div>
            <a href="https://www.helloasso.com/associations/blablacooks/evenements/atelier-cuisine-et-balade-fusion-cambodge-occitanie" target="_blank" class="btn btn-event btn-terracotta">
              S'inscrire
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/>
                <polyline points="15 3 21 3 21 9"/>
                <line x1="10" y1="14" x2="21" y2="3"/>
              </svg>
            </a>
          </div>
        </article>

        <!-- Event 2: Couture Été -->
        <article class="event-card" data-animate>
          <div class="event-image">
            <img src="/vrais-photos/couture-atelier.webp" alt="Atelier Couture Interculturel l'Été" loading="lazy" />
            <div class="event-status status-ongoing">À venir</div>
          </div>
          <div class="event-body">
            <div class="event-meta">
              <span class="event-category cat-couture">
                <img src="/icons/noun-sew-4331889.svg" alt="" class="event-icon" />
                Couture
              </span>
              <span class="event-date">29 Mai → 3 Juil</span>
            </div>
            <h3 class="event-title">Couture Interculturelle — l'Été</h3>
            <p class="event-desc">5 vendredis pour s'initier à la couture dans un espace de rencontre interculturelle. Avec Irène Velay.</p>
            <div class="event-details">
              <div class="detail">
                <img src="/icons/horaire.svg" alt="" class="detail-icon" />
                Vendredis, 9h-12h
              </div>
              <div class="detail">
                <img src="/icons/noun-location-8077585.svg" alt="" class="detail-icon" />
                MPT Voltaire
              </div>
            </div>
            <a href="https://www.helloasso.com/associations/blablacooks/evenements/atelier-de-couture-interculturel-l-ete" target="_blank" class="btn btn-event btn-gold">
              S'inscrire
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/>
                <polyline points="15 3 21 3 21 9"/>
                <line x1="10" y1="14" x2="21" y2="3"/>
              </svg>
            </a>
          </div>
        </article>

        <!-- Event 3: Festin Soudanais et Afghan -->
        <article class="event-card" data-animate>
          <div class="event-image">
            <img src="/vrais-photos/ateliers/afghan-2025/3.jpg" alt="Festin Soudanais et Afghan" loading="lazy" />
            <div class="event-status status-cuisine">7 Juin</div>
          </div>
          <div class="event-body">
            <div class="event-meta">
              <span class="event-category cat-cuisine">
                <img src="/icons/noun-cooking-pot-6972194.svg" alt="" class="event-icon" />
                Cuisine
              </span>
              <span class="event-date">7 Juin · 10h45-14h</span>
            </div>
            <h3 class="event-title">Festin Soudanais et Afghan</h3>
            <p class="event-desc">Aswada et Khanom avec Yousriya Mohamad et Farzana Nazary. Entrée soudanaise et raviolis afghans.</p>
            <div class="event-details">
              <div class="detail">
                <img src="/icons/noun-location-8077585.svg" alt="" class="detail-icon" />
                La Halle
              </div>
            </div>
            <a href="https://www.helloasso.com/associations/blablacooks/evenements/atelier-de-cuisine-festin-vege-soudanais-et-afghan" target="_blank" class="btn btn-event btn-terracotta">
              S'inscrire
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/>
                <polyline points="15 3 21 3 21 9"/>
                <line x1="10" y1="14" x2="21" y2="3"/>
              </svg>
            </a>
          </div>
        </article>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════════
       PAST EVENTS - Recent workshops with details
  ═══════════════════════════════════════════════════════════════════ -->
  <section class="past-events section">
    <div class="container">
      <div class="section-header" data-animate>
        <span class="label label-sage">Retour en images</span>
        <h2>Nos derniers événements</h2>
      </div>

      <div class="past-slider">
        <button class="slider-btn slider-prev" aria-label="Précédent">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M15 18l-6-6 6-6"/></svg>
        </button>
        <div class="past-track">
        <!-- Event 0 - Cuisine Palestinienne -->
        <article class="past-card" data-animate>
          <div class="past-image">
            <img src="/vrais-photos/cuisine-palestinienne-houmous.jpg" alt="Cuisine Palestinienne Végétarienne" class="pos-center" loading="lazy" />
          </div>
          <div class="past-body">
            <span class="past-date">22 Mars 2026</span>
            <h3>Cuisine Palestinienne Végétarienne</h3>
            <p>Soupe de lentilles corail, houmous, moutabal et kunefe au fromage et à l'eau de rose. Avec Sawal, co-animé par Hinde Massaoudi.</p>
          </div>
        </article>

        <!-- Event 1 - Brunch Camerounais -->
        <article class="past-card" data-animate>
          <div class="past-image">
            <img src="/vrais-photos/ateliers/camerounais-2026/6.jpg" alt="Brunch Camerounais" class="pos-center" loading="lazy" />
          </div>
          <div class="past-body">
            <span class="past-date">8 Février 2026</span>
            <h3>Brunch Camerounais</h3>
            <p>Beignets de haricots croustillants et bouillie crémeuse, tradition matinale d'Afrique de l'Ouest. Avec Adèle Nguelefack.</p>
          </div>
        </article>

        <!-- Event 1 -->
        <article class="past-card" data-animate>
          <div class="past-image">
            <img src="/vrais-photos/ateliers/afghan-2025/3.jpg" alt="Mantus Afghans" class="pos-center" loading="lazy" />
          </div>
          <div class="past-body">
            <span class="past-date">7 Décembre 2025</span>
            <h3>Brunch de Fête Afghan</h3>
            <p>Mantus, raviolis à la viande et aux épices avec sauce au yaourt, et ferni aux pistaches. Avec Fawzia Rahmani.</p>
          </div>
        </article>

        <!-- Event 2 -->
        <article class="past-card" data-animate>
          <div class="past-image">
            <img src="/vrais-photos/atelier ukrainien passé2.jpeg" alt="Festin Ukrainien et Géorgien" class="pos-center" loading="lazy" />
          </div>
          <div class="past-body">
            <span class="past-date">12 Octobre 2025</span>
            <h3>Festin Ukrainien & Géorgien</h3>
            <p>Salade d'automne, sauce rouge épicée aux prunes, tortore et roulés d'aubergines aux noix. Avec Natalia Bieliakova.</p>
          </div>
        </article>

        <!-- Event 3 -->
        <article class="past-card" data-animate>
          <div class="past-image">
            <img src="/vrais-photos/atelier-camerounais.jpg" alt="Cuisine Camerounaise" class="pos-center" loading="lazy" />
          </div>
          <div class="past-body">
            <span class="past-date">21 Septembre 2025</span>
            <h3>Cuisine Camerounaise</h3>
            <p>Poulet DG à la banane plantain et aux épices, accompagné d'un bissap hibiscus et gingembre. Avec Yaya Ngouowoud.</p>
          </div>
        </article>

        <!-- Event 4 -->
        <article class="past-card" data-animate>
          <div class="past-image">
            <img src="/vrais-photos/atelier-bresilien.jpg" alt="Cuisine Brésilienne" class="pos-center" loading="lazy" />
          </div>
          <div class="past-body">
            <span class="past-date">18 Mai 2025</span>
            <h3>Cuisine Brésilienne</h3>
            <p>Moqueca de Salvador de Bahia, accompagnée de riz, Farofa et vinegrate. Avec Eloa Sena et Anaïs Josy.</p>
          </div>
        </article>

        </div><!-- end past-track -->
        <button class="slider-btn slider-next" aria-label="Suivant">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M9 18l6-6-6-6"/></svg>
        </button>
      </div><!-- end past-slider -->
      <div class="slider-dots" id="past-dots"></div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════════
       CTA SECTION - Join us
  ═══════════════════════════════════════════════════════════════════ -->
  <section class="cta-section" id="contact">

    <div class="container">
      <div class="cta-content" data-animate>
        <div class="cta-icon">
          <img src="/icons/noun-group-8132009.svg" alt="" />
        </div>

        <h2>Prêt à rejoindre<br/>l'aventure ?</h2>
        <p>Les ateliers qui font parler. Favoriser la création de liens interculturels en français par le partage des richesses culturelles et des savoir-faire.</p>

        <div class="cta-actions">
          <a href="/ateliers-cuisine" class="btn btn-primary">
            Voir les ateliers cuisine
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
          </a>
          <a href="mailto:blablacooks@gmail.com" class="btn btn-secondary">
            Nous contacter
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
              <polyline points="22,6 12,13 2,6"/>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </section>

</Layout>

<script>
  const track = document.querySelector('.past-track') as HTMLElement;
  const prevBtn = document.querySelector('.slider-prev') as HTMLButtonElement;
  const nextBtn = document.querySelector('.slider-next') as HTMLButtonElement;
  const dotsContainer = document.getElementById('past-dots');
  const cards = Array.from(track?.querySelectorAll('.past-card') ?? []) as HTMLElement[];

  const PER_PAGE = 2;

  if (track && cards.length) {
    const numPages = Math.ceil(cards.length / PER_PAGE);

    // Build dots (one per page)
    for (let i = 0; i < numPages; i++) {
      const dot = document.createElement('button');
      dot.className = 'slider-dot' + (i === 0 ? ' active' : '');
      dot.setAttribute('aria-label', `Page ${i + 1}`);
      dot.addEventListener('click', () => scrollToPage(i));
      dotsContainer?.appendChild(dot);
    }

    const dots = Array.from(dotsContainer?.querySelectorAll('.slider-dot') ?? []) as HTMLElement[];

    function getCurrentPage() {
      const cardWidth = cards[0].offsetWidth;
      const gap = 16;
      return Math.round(track.scrollLeft / ((cardWidth + gap) * PER_PAGE));
    }

    function scrollToPage(p: number) {
      const cardIdx = p * PER_PAGE;
      const card = cards[Math.min(cardIdx, cards.length - 1)];
      track.scrollTo({ left: card.offsetLeft, behavior: 'smooth' });
    }

    function updateState() {
      const p = getCurrentPage();
      dots.forEach((d, j) => d.classList.toggle('active', j === p));
      if (prevBtn) prevBtn.disabled = p === 0;
      if (nextBtn) nextBtn.disabled = p >= numPages - 1;
    }

    prevBtn?.addEventListener('click', () => scrollToPage(Math.max(0, getCurrentPage() - 1)));
    nextBtn?.addEventListener('click', () => scrollToPage(Math.min(numPages - 1, getCurrentPage() + 1)));
    track.addEventListener('scroll', updateState, { passive: true });
    updateState();
  }
</script>

<style>
  /* ═══════════════════════════════════════════════════════════════════
     HERO SECTION
  ═══════════════════════════════════════════════════════════════════ */
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: calc(80px + var(--space-xl)) 0 var(--space-xl);
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .hero-bg .blob-1 {
    width: 600px;
    height: 600px;
    top: -100px;
    right: -200px;
  }

  .hero-bg .blob-2 {
    width: 400px;
    height: 400px;
    bottom: -50px;
    left: -100px;
  }

  .hero-pattern {
    position: absolute;
    top: 0;
    right: 0;
    width: 50%;
    height: 100%;
    background-image: radial-gradient(circle at 2px 2px, var(--color-sand) 1px, transparent 1px);
    background-size: 40px 40px;
    opacity: 0.4;
    mask-image: linear-gradient(to left, black, transparent);
  }

  .hero-container {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 var(--space-md);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-xl);
    align-items: center;
  }

  .hero-content {
    position: relative;
    z-index: 2;
  }

  .hero-label {
    display: inline-block;
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--color-terracotta);
    letter-spacing: 0.05em;
    margin-bottom: var(--space-md);
  }

  .hero-title {
    margin-bottom: var(--space-md);
  }

  .hero-title-accent {
    color: var(--color-terracotta);
    font-style: italic;
  }

  .hero-desc {
    font-size: 1.25rem;
    color: var(--color-brown);
    margin-bottom: var(--space-lg);
    max-width: 480px;
  }

  .hero-actions {
    display: flex;
    gap: var(--space-sm);
    flex-wrap: wrap;
    margin-bottom: var(--space-xl);
  }

  .hero-stats {
    display: flex;
    align-items: center;
    gap: var(--space-md);
  }

  .stat {
    display: flex;
    flex-direction: column;
  }

  .stat-number {
    font-family: var(--font-display);
    font-size: 2rem;
    font-weight: 600;
    color: var(--color-brown-dark);
    line-height: 1;
  }

  .stat-label {
    font-size: 0.85rem;
    color: var(--color-taupe);
    margin-top: 0.25rem;
  }

  .stat-divider {
    width: 1px;
    height: 40px;
    background: var(--color-sand);
  }

  /* Hero Visual - Character Composition */
  .hero-visual {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .hero-characters {
    position: relative;
    z-index: 2;
  }

  .hero-characters {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-sm);
  }

  .char-row {
    display: flex;
    justify-content: center;
    gap: var(--space-md);
  }

  .char-row-bottom {
    margin-top: -20px;
  }

  .char {
    filter: drop-shadow(0 10px 25px rgba(92, 74, 61, 0.15));
    transition: transform 0.4s var(--ease-out-expo);
  }

  .char:hover {
    z-index: 10;
  }

  .char img {
    width: 240px;
    height: auto;
  }

  /* Hero decorations */
  .hero-decor {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    pointer-events: none;
  }

  .decor-circle-bg {
    width: 420px;
    height: 420px;
    border-radius: 50%;
    border: 2px dashed var(--color-sand);
    opacity: 0.5;
  }

  /* Scroll indicator */
  .scroll-indicator {
    position: absolute;
    bottom: var(--space-lg);
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
  }

  .scroll-indicator span {
    font-size: 0.75rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--color-taupe);
  }

  .scroll-line {
    width: 1px;
    height: 40px;
    background: linear-gradient(to bottom, var(--color-terracotta), transparent);
    animation: scroll-pulse 2s ease-in-out infinite;
  }

  @keyframes scroll-pulse {
    0%, 100% { opacity: 1; transform: scaleY(1); }
    50% { opacity: 0.5; transform: scaleY(0.6); }
  }

  /* ═══════════════════════════════════════════════════════════════════
     MISSION SECTION
  ═══════════════════════════════════════════════════════════════════ */
  .mission {
    position: relative;
    background: var(--color-terracotta);
    padding: var(--space-3xl) 0;
    overflow: hidden;
  }

  .mission-bg {
    position: absolute;
    inset: 0;
  }

  .mission-content {
    position: relative;
    text-align: center;
    max-width: 800px;
    margin: 0 auto var(--space-2xl);
    color: white;
  }

  .mission-icon {
    display: flex;
    justify-content: center;
    margin-bottom: var(--space-md);
    opacity: 0.85;
  }

  .mission-icon img {
    filter: brightness(0) invert(1) sepia(1) saturate(0.5) hue-rotate(10deg);
  }

  .mission-title {
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    font-weight: 500;
    color: white;
    margin-bottom: var(--space-md);
  }

  .mission .divider {
    background: linear-gradient(90deg, transparent, var(--color-gold-light), transparent);
  }

  .mission-text {
    font-size: 1.15rem;
    line-height: 1.8;
    opacity: 0.9;
    max-width: 650px;
    margin: 0 auto;
  }

  /* Photo mosaic */
  .mission-photos {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: var(--space-md);
    max-width: 1100px;
    margin: 0 auto;
  }

  .photo-card {
    position: relative;
    border-radius: var(--radius-lg);
    overflow: hidden;
    aspect-ratio: 3/4;
    transition: transform 0.5s var(--ease-out-expo);
  }

  .photo-card img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    transition: transform 0.6s var(--ease-out-expo);
  }

  .photo-card:hover {
    box-shadow: 0 4px 20px rgba(92, 74, 61, 0.12);
  }

  .photo-card:hover img {
  }

  .photo-1 { transform: rotate(-2deg); }
  .photo-2 { transform: translateY(16px); }
  .photo-3 { transform: rotate(1.5deg); }

  .photo-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(61, 48, 41, 0.3), transparent);
  }

  /* ═══════════════════════════════════════════════════════════════════
     ACTIVITIES SECTION
  ═══════════════════════════════════════════════════════════════════ */
  .activities {
    background: var(--color-cream);
    padding-bottom: var(--space-xl);
  }

  .section-header {
    text-align: center;
    margin-bottom: var(--space-2xl);
  }

  .section-header h2 {
    margin-top: var(--space-sm);
  }

  .section-desc {
    color: var(--color-brown);
    opacity: 0.7;
    margin-top: var(--space-xs);
    text-align: center;
    margin-left: auto;
    margin-right: auto;
  }

  .activities-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-lg);
  }

  .activities-grid .activity-card:first-child {
    grid-row: span 2;
  }

  .activities-grid .activity-card:first-child .activity-visual {
    flex: 1;
    height: auto;
    min-height: 280px;
  }

  .activities-grid .activity-card:not(:first-child) .activity-visual {
    height: 150px;
  }

  .activity-card {
    display: flex;
    flex-direction: column;
    background: white;
    border-radius: var(--radius-xl);
    overflow: visible;
    border: 1px solid #EDE8E3;
    transition: all 0.4s var(--ease-out-expo);
  }

  .activity-card:hover {
    border-color: #D0C8C0;
  }

  .activity-visual {
    position: relative;
    height: 220px;
    overflow: hidden;
    border-radius: 12px;
    margin: 12px 12px 0;
  }

  .activity-img-wrapper {
    position: absolute;
    inset: 0;
  }

  .activity-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s var(--ease-out-expo);
  }

  .activity-card:hover .activity-img {
    transform: scale(1.015);
  }

  .activity-badge {
    position: absolute;
    top: var(--space-sm);
    left: var(--space-sm);
    padding: 0.5rem 1rem;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    border-radius: var(--radius-full);
    backdrop-filter: blur(10px);
  }

  .activity-cuisine .activity-badge,
  .activity-couture .activity-badge,
  .activity-francais .activity-badge {
    background: rgba(255, 255, 255, 0.85);
    color: var(--color-brown-dark);
  }

  .activity-content {
    padding: var(--space-lg);
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  .activity-content h3 {
    font-size: 1.5rem;
    margin-bottom: var(--space-xs);
  }

  .activity-content p {
    color: var(--color-brown);
    opacity: 0.75;
    font-size: 1rem;
    line-height: 1.6;
    margin-bottom: var(--space-md);
  }

  .activity-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: var(--space-md);
  }

  .activity-tags span {
    font-size: 0.75rem;
    padding: 0.35rem 0.75rem;
    background: var(--color-cream-dark);
    color: var(--color-brown);
    border-radius: var(--radius-full);
  }

  .activity-link {
    margin-top: auto;
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    font-weight: 600;
    font-size: 0.9rem;
    text-decoration: underline;
    text-underline-offset: 0.22em;
    transition: gap 0.25s ease;
  }

  .activity-cuisine .activity-link { color: var(--color-terracotta); }
  .activity-couture .activity-link { color: var(--color-gold); }
  .activity-francais .activity-link { color: var(--color-dusty-blue); }

  .activity-link .arrow {
    display: inline-block;
    transition: transform 0.2s ease;
  }

  .activity-card:hover .activity-link .arrow {
    transform: translateX(5px);
  }

  /* ═══════════════════════════════════════════════════════════════════
     UPCOMING EVENTS SECTION
  ═══════════════════════════════════════════════════════════════════ */
  .upcoming {
    position: relative;
    background: var(--color-cream-dark);
    overflow: hidden;
  }

  .upcoming-bg {
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .upcoming-pattern {
    position: absolute;
    inset: 0;
    background-image: radial-gradient(circle at 1px 1px, var(--color-sand) 1px, transparent 1px);
    background-size: 30px 30px;
    opacity: 0.5;
  }

  .section-header-light h2 {
    color: var(--color-brown-dark);
  }

  .events-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-lg);
    max-width: 1100px;
    margin: 0 auto;
  }

  .event-card {
    position: relative;
    background: white;
    border-radius: var(--radius-lg);
    overflow: hidden;
    border: 1px solid #EDE8E3;
    transition: all 0.4s var(--ease-out-expo);
    display: flex;
    flex-direction: column;
  }

  .event-card:hover {
    border-color: #D0C8C0;
  }

  .event-image {
    position: relative;
    aspect-ratio: 16/10;
    overflow: hidden;
    border-radius: 10px;
    margin: 10px 10px 0;
  }

  .event-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s var(--ease-out-expo);
  }

  .event-card:hover .event-image img {
    transform: scale(1.015);
  }

  .event-status {
    position: absolute;
    top: var(--space-sm);
    right: var(--space-sm);
    padding: 0.4rem 0.9rem;
    font-size: 0.7rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    border-radius: var(--radius-full);
    color: white;
  }

  .status-ongoing { background: var(--color-gold); }
  .status-new { background: var(--color-dusty-blue); }
  .status-cuisine { background: var(--color-terracotta); }

  .event-body {
    padding: var(--space-md);
    display: flex;
    flex-direction: column;
    flex: 1;
  }

  .event-meta {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--space-sm);
  }

  .event-category {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    padding: 0.2rem 0.65rem;
    border-radius: 100px;
    font-size: 0.75rem;
    font-weight: 600;
  }

  .cat-cuisine,
  .cat-couture,
  .cat-francais {
    color: var(--color-brown-dark);
    background: var(--color-cream-dark);
  }

  .event-icon {
    width: 16px;
    height: 16px;
  }

  .cat-cuisine .event-icon {
    filter: invert(40%) sepia(60%) saturate(800%) hue-rotate(340deg) brightness(90%) contrast(90%);
  }

  .cat-couture .event-icon {
    filter: invert(70%) sepia(50%) saturate(500%) hue-rotate(5deg) brightness(95%) contrast(90%);
  }

  .cat-francais .event-icon {
    filter: invert(65%) sepia(15%) saturate(500%) hue-rotate(175deg) brightness(90%) contrast(85%);
  }

  .event-date {
    display: inline-flex;
    align-items: center;
    padding: 0.2rem 0.65rem;
    background: var(--color-cream-dark);
    border-radius: 100px;
    font-size: 0.75rem;
    font-weight: 500;
    color: var(--color-taupe);
  }

  .event-title {
    font-size: 1.25rem;
    margin-bottom: var(--space-xs);
  }

  .event-desc {
    font-size: 0.95rem;
    color: var(--color-brown);
    opacity: 0.75;
    line-height: 1.6;
    margin-bottom: var(--space-sm);
  }

  .event-tags-dietary {
    display: flex;
    gap: 0.5rem;
    margin-bottom: var(--space-sm);
  }

  .event-tags-dietary span {
    font-size: 0.7rem;
    padding: 0.3rem 0.6rem;
    background: var(--color-sage-light);
    color: var(--color-sage);
    border-radius: var(--radius-full);
    font-weight: 500;
  }

  .event-details {
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    gap: 0.4rem;
    margin-bottom: var(--space-md);
  }

  .detail {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.2rem 0.65rem;
    background: var(--color-cream-dark);
    border-radius: 100px;
    font-size: 0.8rem;
    color: var(--color-brown);
  }

  .detail-icon {
    width: 16px;
    height: 16px;
    opacity: 0.7;
    filter: invert(40%) sepia(60%) saturate(800%) hue-rotate(340deg) brightness(90%) contrast(90%);
  }

  .detail svg {
    opacity: 0.6;
  }

  .btn-event {
    width: 100%;
    justify-content: center;
    padding: 0.9rem 1.5rem;
    font-size: 0.9rem;
    border-radius: var(--radius-md);
    margin-top: auto;
  }

  .btn-terracotta {
    background: var(--color-terracotta);
    color: white;
  }

  .btn-terracotta:hover {
    background: var(--color-terracotta-dark);
  }

  .btn-gold {
    background: var(--color-gold);
    color: white;
  }

  .btn-gold:hover {
    background: #c4941f;
  }

  .btn-blue {
    background: var(--color-dusty-blue);
    color: white;
  }

  .btn-blue:hover {
    background: #6a8799;
  }

  /* ═══════════════════════════════════════════════════════════════════
     PAST EVENTS SECTION
  ═══════════════════════════════════════════════════════════════════ */
  .past-events {
    background: var(--color-cream);
    padding-top: var(--space-xl);
  }

  .past-slider {
    position: relative;
    max-width: 1060px;
    margin: 0 auto;
    padding: 0 52px;
  }

  .past-track {
    display: flex;
    gap: 16px;
    overflow-x: scroll;
    scroll-snap-type: x mandatory;
    scroll-behavior: smooth;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
    border-radius: var(--radius-xl);
  }

  .past-track::-webkit-scrollbar {
    display: none;
  }

  .slider-btn {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    z-index: 10;
    background: var(--color-terracotta);
    border: none;
    border-radius: 50%;
    width: 40px;
    height: 40px;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    transition: all 0.2s ease;
    box-shadow: 0 2px 8px rgba(196, 87, 59, 0.3);
  }

  .slider-btn:hover {
    background: var(--color-terracotta-dark);
  }

  .slider-btn:disabled {
    opacity: 0.3;
    cursor: default;
  }

  .slider-prev { left: 0; }
  .slider-next { right: 0; }

  .slider-dots {
    display: flex;
    justify-content: center;
    gap: 6px;
    margin-top: var(--space-md);
  }

  .slider-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--color-sand);
    border: none;
    cursor: pointer;
    padding: 0;
    transition: all 0.2s ease;
  }

  .slider-dot.active {
    background: var(--color-terracotta);
    width: 20px;
    border-radius: 3px;
  }

  .past-card {
    scroll-snap-align: start;
    flex: 0 0 calc(50% - 8px);
    position: relative;
    border-radius: var(--radius-xl);
    overflow: hidden;
    aspect-ratio: 4/3;
  }

  .past-card::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(20, 12, 8, 0.78) 0%, rgba(20, 12, 8, 0.35) 50%, transparent 80%);
    z-index: 1;
  }

  .past-image {
    position: absolute;
    inset: 0;
    overflow: hidden;
  }

  .past-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s var(--ease-out-expo);
  }

  .past-image img.pos-top { object-position: top; }
  .past-image img.pos-center { object-position: center; }
  .past-image img.pos-bottom { object-position: center; }

  .past-card:hover .past-image img {
    transform: scale(1.02);
  }

  .past-body {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: var(--space-md) var(--space-lg);
    z-index: 2;
  }

  .past-date {
    display: inline-flex;
    align-items: center;
    padding: 0.2rem 0.65rem;
    background: rgba(255, 255, 255, 0.18);
    color: white;
    border-radius: 100px;
    font-size: 0.75rem;
    font-weight: 600;
    margin-bottom: var(--space-sm);
    backdrop-filter: blur(6px);
  }

  .past-body h3 {
    font-size: clamp(1rem, 1.8vw, 1.4rem);
    color: white;
    margin-bottom: var(--space-xs);
    line-height: 1.2;
  }

  .past-body p {
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.82);
    line-height: 1.5;
    max-width: 600px;
  }

  /* ═══════════════════════════════════════════════════════════════════
     CTA SECTION
  ═══════════════════════════════════════════════════════════════════ */
  .cta-section {
    position: relative;
    background: white;
    padding: var(--space-3xl) 0;
    overflow: hidden;
  }

  .cta-bg {
    position: absolute;
    inset: 0;
    pointer-events: none;
  }

  .cta-shape {
    position: absolute;
    border-radius: 50%;
    background: linear-gradient(135deg, rgba(196, 87, 59, 0.2), rgba(212, 161, 74, 0.1));
  }

  .cta-shape-1 {
    width: 500px;
    height: 500px;
    top: -200px;
    right: -100px;
  }

  .cta-shape-2 {
    width: 300px;
    height: 300px;
    bottom: -100px;
    left: -50px;
  }

  .cta-content {
    position: relative;
    text-align: center;
    color: var(--color-brown-dark);
  }

  .cta-icon {
    display: flex;
    justify-content: center;
    margin-bottom: var(--space-md);
  }

  .cta-icon img {
    width: 64px;
    height: 64px;
    filter: invert(40%) sepia(60%) saturate(800%) hue-rotate(340deg) brightness(90%) contrast(90%);
  }

  .cta-content h2 {
    font-size: clamp(2rem, 5vw, 3rem);
    color: var(--color-terracotta);
    margin-bottom: var(--space-sm);
  }

  .cta-content p {
    font-size: 1.15rem;
    opacity: 0.7;
    max-width: 500px;
    margin: 0 auto var(--space-lg);
  }

  .cta-actions {
    display: flex;
    justify-content: center;
    gap: var(--space-sm);
    flex-wrap: wrap;
  }

  /* ═══════════════════════════════════════════════════════════════════
     RESPONSIVE
  ═══════════════════════════════════════════════════════════════════ */
  @media (max-width: 1024px) {
    .hero-container {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .hero-content {
      max-width: 600px;
      margin: 0 auto;
    }

    .hero-desc {
      margin-left: auto;
      margin-right: auto;
    }

    .hero-actions {
      justify-content: center;
    }

    .hero-stats {
      justify-content: center;
    }

    .hero-visual {
      display: none;
    }

    .activities-grid {
      grid-template-columns: 1fr;
      max-width: 500px;
      margin: 0 auto;
    }

    .activities-grid .activity-card:first-child {
      grid-row: span 1;
    }

    .activities-grid .activity-card:first-child .activity-visual,
    .activities-grid .activity-card:not(:first-child) .activity-visual {
      height: 220px;
      flex: none;
    }

    .events-grid {
      grid-template-columns: repeat(2, 1fr);
      max-width: 700px;
      margin: 0 auto;
    }

  }

  @media (max-width: 768px) {
    .hero {
      min-height: auto;
      padding-top: 120px;
      padding-bottom: var(--space-xl);
    }

    .scroll-indicator {
      display: none;
    }

    .mission-photos {
      grid-template-columns: 1fr;
      max-width: 350px;
    }

    .photo-1, .photo-2, .photo-3 {
      transform: none;
    }

    .slider-btn {
      display: none;
    }
  }
</style>
